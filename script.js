const translations = {
    en: {
        together: "Together with their families",
        names: "Mounika & Sai Kishore",
        bride_details: "Daughter of Smt. Ramanamma & Sri Nallamari Rama Mohan",
        groom_details: "Eldest Son of Smt. Padma Priya & Sri Sankarapu Sreenivasulu",
        invite_text: "Invite you to celebrate their wedding",
        wedding_date: "17 • June • 2026",
        wedding_time_exact: "Time: 4:15 AM - 5:15 AM",
        wedding_location: "Sri Chowdeshwari Wedding Venue, Madanapalle",
        days: "Days",
        hours: "Hrs",
        minutes: "Min",
        seconds: "Sec",
        our_story: "Our Story",
        story_1: "The first time we met...",
        story_2: "She said yes!",
        events_title: "Wedding Events",
        reception: "Reception",
        reception_date: "June 16, 2026 | 7:00 PM",
        reception_loc: "Sri Chowdeshwari Wedding Venue, Madanapalle",
        wedding: "The Wedding",
        wedding_time: "June 17, 2026 | 4:15 AM",
        get_directions: "Get Directions",
        add_calendar: "Add to Calendar",
        footer: "Made with ❤️ for Mounika & Sai Kishore",
        warm_wishes: "Warm Wishes",
        invite_parents: "We cordially invite you to grace the auspicious occasion of our daughter's wedding and bless the couple.",
        author_parents: "- Smt. Ramanamma & Sri Nallamari Rama Mohan (Parents)",
        invite_anna: "Please join us in celebrating my little sister's wedding!",
        author_anna: "- Venu Gopal (Elder Brother)",
        invite_thammudu: "Looking forward to your presence at my sister's wedding celebration!",
        author_thammudu: "- Krishna Vamsi (Younger Brother)"
    },
    te: {
        together: "వారి కుటుంబ సభ్యులతో కలిసి",
        names: "మౌనిక & సాయి కిషోర్",
        bride_details: "శ్రీమతి రమణమ్మ మరియు శ్రీ నల్లమారి రామ మోహన్ గారి కుమార్తె",
        groom_details: "శ్రీమతి పద్మప్రియ మరియు శ్రీ సంకరాపు శ్రీనివాసులు గారి జ్యేష్ఠ కుమారుడు",
        invite_text: "వారి వివాహానికి మిమ్మల్ని ఆహ్వానిస్తున్నారు",
        wedding_date: "17 • జూన్ • 2026",
        wedding_time_exact: "సమయం: వేకువజామున 4:15 - 5:15 వరకు",
        wedding_location: "శ్రీ చౌడేశ్వరి కళ్యాణ మండపం, మదనపల్లె",
        days: "రోజులు",
        hours: "గంటలు",
        minutes: "నిమిషాలు",
        seconds: "సెకన్లు",
        our_story: "మా కథ",
        story_1: "మొదటి పరిచయం...",
        story_2: "ఆమె అంగీకరించింది!",
        events_title: "వివాహ వేడుకలు",
        reception: "రిసెప్షన్",
        reception_date: "జూన్ 16, 2026 | రాత్రి 7:00 గంటలకు",
        reception_loc: "శ్రీ చౌడేశ్వరి కళ్యాణ మండపం, మదనపల్లె",
        wedding: "వివాహం",
        wedding_time: "జూన్ 17, 2026 | వేకువజామున 4:15 గంటలకు",
        get_directions: "దారి తెలుసుకోండి",
        add_calendar: "క్యాలెండర్‌కు జోడించు",
        footer: "మౌనిక మరియు సాయి కిషోర్ కోసం ❤️ తో తయారు చేయబడింది",
        warm_wishes: "శుభాకాంక్షలు",
        invite_parents: "మా కుమార్తె వివాహ మహోత్సవానికి విచ్చేసి వధూవరులను ఆశీర్వదించవలసిందిగా ప్రార్థిస్తున్నాము.",
        author_parents: "- శ్రీమతి రమణమ్మ మరియు శ్రీ నల్లమారి రామ మోహన్ (తల్లిదండ్రులు)",
        invite_anna: "మా చెల్లి పెళ్లికి తప్పక రావాలి!",
        author_anna: "- వేణు గోపాల్ (అన్న)",
        invite_thammudu: "మా అక్క పెళ్లి సందడికి మీరే ప్రత్యేకం!",
        author_thammudu: "- కృష్ణ వంశీ (తమ్ముడు)"
    },
    hi: {
        together: "अपने परिवारों के साथ",
        names: "मौनिका और साई किशोर",
        bride_details: "श्रीमती रमणम्मा और श्री नल्लमरी राम मोहन की पुत्री",
        groom_details: "श्रीमती पद्मप्रिया और श्री संकरापु श्रीनिवासुलु के ज्येष्ठ पुत्र",
        invite_text: "आपको उनके विवाह समारोह में आमंत्रित करते हैं",
        wedding_date: "17 • जून • 2026",
        wedding_time_exact: "समय: सुबह 4:15 - 5:15 बजे",
        wedding_location: "श्री चौडेश्वरी विवाह स्थल, मदनपल्ले",
        days: "दिन",
        hours: "घंटे",
        minutes: "मिनट",
        seconds: "सेकंड",
        our_story: "हमारी कहानी",
        story_1: "हमारी पहली मुलाकात...",
        story_2: "उसने हाँ कह दिया!",
        events_title: "शादी के समारोह",
        reception: "रिसेप्शन",
        reception_date: "16 जून, 2026 | शाम 7:00 बजे",
        reception_loc: "श्री चौडेश्वरी विवाह स्थल, मदनपल्ले",
        wedding: "शादी",
        wedding_time: "17 जून, 2026 | सुबह 4:15 बजे",
        get_directions: "रास्ता देखें",
        add_calendar: "कैलेंडर में जोड़ें",
        footer: "मौनिका और साई किशोर के लिए ❤️ से बना",
        warm_wishes: "शुभकामनाएं",
        invite_parents: "हम आपको हमारी बेटी की शादी के शुभ अवसर पर आमंत्रित करते हैं, कृपया आकर नवविवाहित जोड़े को आशीर्वाद दें।",
        author_parents: "- श्रीमती रमणम्मा और श्री नल्लमरी राम मोहन (माता-पिता)",
        invite_anna: "मेरी छोटी बहन की शादी में जरूर आएं!",
        author_anna: "- वेणु गोपाल (बड़ा भाई)",
        invite_thammudu: "मेरी बड़ी बहन की शादी के जश्न में आपकी उपस्थिति का इंतजार है!",
        author_thammudu: "- कृष्ण वामसी (छोटा भाई)"
    },
    kn: {
        together: "ತಮ್ಮ ಕುಟುಂಬದವರೊಂದಿಗೆ",
        names: "ಮೌನಿಕಾ ಮತ್ತು ಸಾಯಿ ಕಿಶೋರ್",
        bride_details: "ಶ್ರೀಮತಿ ರಮಣಮ್ಮ ಮತ್ತು ಶ್ರೀ ನಲ್ಲಮರಿ ರಾಮ ಮೋಹನ್ ಅವರ ಪುತ್ರಿ",
        groom_details: "ಶ್ರೀಮತಿ ಪದ್ಮಪ್ರಿಯಾ ಮತ್ತು ಶ್ರೀ ಸಂಕರಾಪು ಶ್ರೀನಿವಾಸುಲು ಅವರ ಜ್ಯೇಷ್ಠ ಪುತ್ರ",
        invite_text: "ತಮ್ಮ ವಿವಾಹಕ್ಕೆ ನಿಮ್ಮನ್ನು ಆಹ್ವಾನಿಸುತ್ತಿದ್ದಾರೆ",
        wedding_date: "17 • ಜೂನ್ • 2026",
        wedding_time_exact: "ಸಮಯ: ಬೆಳಗಿನ ಜಾವ 4:15 - 5:15",
        wedding_location: "ಶ್ರೀ ಚೌಡೇಶ್ವರಿ ವಿವಾಹ ಮಂಟಪ, ಮದನಪಲ್ಲೆ",
        days: "ದಿನಗಳು",
        hours: "ಗಂಟೆಗಳು",
        minutes: "ನಿಮಿಷಗಳು",
        seconds: "ಸೆಕೆಂಡುಗಳು",
        our_story: "ನಮ್ಮ ಕಥೆ",
        story_1: "ಮೊದಲ ಭೇಟಿ...",
        story_2: "ಅವಳು ಒಪ್ಪಿಕೊಂಡಳು!",
        events_title: "ವಿವಾಹ ಸಮಾರಂಭಗಳು",
        reception: "ರಿಸೆಪ್ಷನ್",
        reception_date: "ಜೂನ್ 16, 2026 | ರಾತ್ರಿ 7:00 ಕ್ಕೆ",
        reception_loc: "ಶ್ರೀ ಚೌಡೇಶ್ವರಿ ವಿವಾಹ ಮಂಟಪ, ಮದನಪಲ್ಲೆ",
        wedding: "ವಿವಾಹ",
        wedding_time: "ಜೂನ್ 17, 2026 | ಬೆಳಗಿನ ಜಾವ 4:15 ಕ್ಕೆ",
        get_directions: "ಮಾರ್ಗವನ್ನು ಪಡೆಯಿರಿ",
        add_calendar: "ಕ್ಯಾಲೆಂಡರ್‌ಗೆ ಸೇರಿಸಿ",
        footer: "ಮೌನಿಕಾ ಮತ್ತು ಸಾಯಿ ಕಿಶೋರ್‌ಗಾಗಿ ❤️ ದಿಂದ ಮಾಡಲಾಗಿದೆ",
        warm_wishes: "ಶುಭಾಶಯಗಳು",
        invite_parents: "ನಮ್ಮ ಮಗಳ ವಿವಾಹ ಮಹೋತ್ಸವಕ್ಕೆ ಆಗಮಿಸಿ ವಧು-ವರರನ್ನು ಆಶೀರ್ವದಿಸಬೇಕಾಗಿ ವಿನಂತಿಸುತ್ತೇವೆ.",
        author_parents: "- ಶ್ರೀಮತಿ ರಮಣಮ್ಮ ಮತ್ತು ಶ್ರೀ ನಲ್ಲಮರಿ ರಾಮ ಮೋಹನ್ (ಪೋಷಕರು)",
        invite_anna: "ನನ್ನ ತಂಗಿಯ ಮದುವೆಗೆ ತಪ್ಪದೇ ಬನ್ನಿ!",
        author_anna: "- ವೇಣು ಗೋಪಾಲ್ (ಅಣ್ಣ)",
        invite_thammudu: "ನನ್ನ ಅಕ್ಕನ ಮದುವೆಯ ಸಂಭ್ರಮದಲ್ಲಿ ನಿಮ್ಮ ಉಪಸ್ಥಿತಿಯನ್ನು ನಿರೀಕ್ಷಿಸುತ್ತಿದ್ದೇವೆ!",
        author_thammudu: "- ಕೃಷ್ಣ ವಂಶಿ (ತಮ್ಮ)"
    },
    ta: {
        together: "தங்கள் குடும்பத்தினருடன் இணைந்து",
        names: "மௌனிகா மற்றும் சாய் கிஷோர்",
        bride_details: "திருமதி ரமணம்மா மற்றும் திரு நல்லமாரி ராம மோகன் அவர்களின் மகள்",
        groom_details: "திருமதி பத்மப்ரியா மற்றும் திரு சங்கராபு சீனிவாசுலு அவர்களின் மூத்த மகன்",
        invite_text: "தங்கள் திருமணத்திற்கு உங்களை அழைக்கிறார்கள்",
        wedding_date: "17 • ஜூன் • 2026",
        wedding_time_exact: "நேரம்: அதிகாலை 4:15 - 5:15",
        wedding_location: "ஸ்ரீ சௌடேஸ்வரி திருமண மண்டபம், மதனப்பள்ளி",
        days: "நாட்கள்",
        hours: "மணி",
        minutes: "நிமிடம்",
        seconds: "விநாடி",
        our_story: "எங்கள் கதை",
        story_1: "முதல் சந்திப்பு...",
        story_2: "அவள் சம்மதித்தாள்!",
        events_title: "திருமண நிகழ்வுகள்",
        reception: "வரவேற்பு",
        reception_date: "ஜூன் 16, 2026 | இரவு 7:00 மணிக்கு",
        reception_loc: "ஸ்ரீ சௌடேஸ்வரி திருமண மண்டபம், மதனப்பள்ளி",
        wedding: "திருமணம்",
        wedding_time: "ஜூன் 17, 2026 | அதிகாலை 4:15 மணிக்கு",
        get_directions: "வழி காண்க",
        add_calendar: "நாட்காட்டியில் சேர்",
        footer: "மௌனிகா மற்றும் சாய் கிஷோருக்காக ❤️ உடன் உருவாக்கப்பட்டது",
        warm_wishes: "வாழ்த்துக்கள்",
        invite_parents: "எங்கள் மகளின் திருமண விழாவிற்கு வருகை தந்து மணமக்களை வாழ்த்த அன்புடன் அழைக்கிறோம்.",
        author_parents: "- திருமதி ரமணம்மா மற்றும் திரு நல்லமாரி ராம மோகன் (பெற்றோர்)",
        invite_anna: "என் தங்கையின் திருமணத்திற்கு கட்டாயம் வரவும்!",
        author_anna: "- வேணு கோபால் (அண்ணா)",
        invite_thammudu: "என் அக்காவின் திருமண கொண்டாட்டத்தில் உங்கள் வருகையை எதிர்நோக்குகிறோம்!",
        author_thammudu: "- கிருஷ்ண வம்சி (தம்பி)"
    }
}

document.addEventListener('DOMContentLoaded', () => {

    // --- 0. Language Selector ---
    const langSelect = document.getElementById('lang-select');
    const updateLanguage = (lang) => {
        const t = translations[lang];
        if (!t) return;

        document.querySelectorAll('[data-i18n]').forEach(el => {
            const key = el.getAttribute('data-i18n');
            if (t[key]) {
                el.innerText = t[key];
            }
        });
    };

    langSelect.addEventListener('change', (e) => {
        updateLanguage(e.target.value);
    });

    // Initialize translations on load
    updateLanguage(langSelect.value);

    // --- 1. Countdown Timer ---
    const weddingDate = new Date('Jun 17, 2026 04:15:00').getTime();

    const updateCountdown = () => {
        const now = new Date().getTime();
        const distance = weddingDate - now;

        if (distance < 0) {
            document.getElementById('countdown').innerHTML = "<h3 style='color: var(--accent-gold);'>The Celebration Has Begun!</h3>";
            return;
        }

        const days = Math.floor(distance / (1000 * 60 * 60 * 24));
        const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
        const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
        const seconds = Math.floor((distance % (1000 * 60)) / 1000);

        document.getElementById('days').innerText = String(days).padStart(2, '0');
        document.getElementById('hours').innerText = String(hours).padStart(2, '0');
        document.getElementById('minutes').innerText = String(minutes).padStart(2, '0');
        document.getElementById('seconds').innerText = String(seconds).padStart(2, '0');
    };

    setInterval(updateCountdown, 1000);
    updateCountdown();

    // --- 2. Scroll Animations (Intersection Observer) ---
    const observerOptions = {
        threshold: 0.1,
        rootMargin: "0px 0px -50px 0px"
    };

    const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                entry.target.classList.add('show');
                observer.unobserve(entry.target);
            }
        });
    }, observerOptions);

    const hiddenElements = document.querySelectorAll('.hidden');
    hiddenElements.forEach(el => observer.observe(el));

    // --- 3. Audio Toggle ---
    const audioBtn = document.getElementById('audio-btn');
    const bgMusic = document.getElementById('bg-music');
    let isPlaying = false;

    bgMusic.volume = 0.4;

    audioBtn.addEventListener('click', () => {
        if (isPlaying) {
            bgMusic.pause();
            audioBtn.classList.remove('playing');
            audioBtn.innerHTML = '<i class="fas fa-music"></i>';
        } else {
            bgMusic.play().catch(error => console.log("Audio play failed:", error));
            audioBtn.classList.add('playing');
            audioBtn.innerHTML = '<i class="fas fa-pause"></i>';
        }
        isPlaying = !isPlaying;
    });



    // --- Splash Screen Logic ---
    const splashScreen = document.getElementById('splash-screen');
    const openBtn = document.getElementById('open-invite-btn');
    const envelope = document.getElementById('envelope');
    const sunburst = document.getElementById('sunburst');

    if (openBtn && envelope && sunburst) {
        openBtn.addEventListener('click', () => {
            // 1. Shrink and fade the envelope
            envelope.classList.add('is-open');

            // 2. After envelope vanishes, trigger the sunburst
            setTimeout(() => {
                sunburst.classList.add('active');
            }, 600);

            // 3. After sunburst fills the screen, fade out and reveal
            setTimeout(() => {
                splashScreen.classList.add('fade-out');
                // Auto-start music
                if (!isPlaying) {
                    bgMusic.play().catch(e => console.log("Audio play failed:", e));
                    audioBtn.classList.add('playing');
                    audioBtn.innerHTML = '<i class="fas fa-pause"></i>';
                    isPlaying = true;
                }
            }, 2600); // 0.6s envelope + 2s sunburst
        });
    }

    // --- Scratch-to-Reveal ---
    const scratchCanvas = document.getElementById('scratch-canvas');
    if (scratchCanvas) {
        const ctx = scratchCanvas.getContext('2d');
        let isScratching = false;
        let scratchRevealed = false;
        let canvasReady = false;
        let scratchCount = 0;

        const initCanvas = () => {
            if (canvasReady) return;
            // Use fixed dimensions matching CSS
            scratchCanvas.width = 340;
            scratchCanvas.height = 220;

            // Golden metallic gradient cover
            const grad = ctx.createLinearGradient(0, 0, 340, 220);
            grad.addColorStop(0, '#b8860b');
            grad.addColorStop(0.3, '#d4af37');
            grad.addColorStop(0.5, '#ffd700');
            grad.addColorStop(0.7, '#d4af37');
            grad.addColorStop(1, '#b8860b');
            ctx.fillStyle = grad;
            ctx.fillRect(0, 0, 340, 220);

            // "Scratch Here" text
            ctx.fillStyle = 'rgba(0,0,0,0.4)';
            ctx.font = 'bold 18px Montserrat, sans-serif';
            ctx.textAlign = 'center';
            ctx.fillText('✨ Scratch Here ✨', 170, 105);
            ctx.font = '12px Montserrat, sans-serif';
            ctx.fillText('Swipe to reveal the date', 170, 125);

            canvasReady = true;
        };

        // Init immediately + also when section becomes visible
        initCanvas();
        const scratchObserver = new IntersectionObserver((entries) => {
            if (entries[0].isIntersecting) initCanvas();
        });
        scratchObserver.observe(scratchCanvas);

        const scratch = (x, y) => {
            if (!canvasReady || scratchRevealed) return;
            // Scale mouse coords to canvas coords
            const rect = scratchCanvas.getBoundingClientRect();
            const scaleX = 340 / rect.width;
            const scaleY = 220 / rect.height;
            const cx = x * scaleX;
            const cy = y * scaleY;

            ctx.globalCompositeOperation = 'destination-out';
            ctx.beginPath();
            ctx.arc(cx, cy, 28, 0, Math.PI * 2);
            ctx.fill();

            // Check reveal every 5 scratches for performance
            scratchCount++;
            if (scratchCount % 5 === 0) checkReveal();
        };

        const getPos = (e) => {
            const rect = scratchCanvas.getBoundingClientRect();
            if (e.touches) {
                return { x: e.touches[0].clientX - rect.left, y: e.touches[0].clientY - rect.top };
            }
            return { x: e.clientX - rect.left, y: e.clientY - rect.top };
        };

        const checkReveal = () => {
            if (scratchRevealed) return;

            // Fallback: If they scratched a lot (e.g. 40 strokes), just reveal it
            // This is useful if the browser blocks getImageData for security reasons
            if (scratchCount > 40) {
                doReveal();
                return;
            }

            try {
                const imageData = ctx.getImageData(0, 0, 340, 220);
                let transparent = 0;
                // Sample every 4th pixel for speed
                for (let i = 3; i < imageData.data.length; i += 16) {
                    if (imageData.data[i] < 128) transparent++; // < 50% opacity
                }
                const total = imageData.data.length / 16;
                if (transparent / total > 0.60) {
                    doReveal();
                }
            } catch (e) {
                // If getImageData fails (CORS/Tainted Canvas), just wait for the fallback stroke count
                console.log("Canvas read blocked, relying on stroke count fallback");
            }
        };

        const doReveal = () => {
            scratchRevealed = true;
            scratchCanvas.style.transition = 'opacity 0.8s ease';
            scratchCanvas.style.opacity = '0';
            setTimeout(() => { scratchCanvas.style.display = 'none'; }, 800);
        };

        // Mouse events
        scratchCanvas.addEventListener('mousedown', (e) => {
            isScratching = true;
            const pos = getPos(e);
            scratch(pos.x, pos.y);
        });
        scratchCanvas.addEventListener('mouseup', () => { isScratching = false; });
        scratchCanvas.addEventListener('mouseleave', () => { isScratching = false; });
        scratchCanvas.addEventListener('mousemove', (e) => {
            if (!isScratching) return;
            const pos = getPos(e);
            scratch(pos.x, pos.y);
        });

        // Touch events
        scratchCanvas.addEventListener('touchstart', (e) => {
            isScratching = true;
            const pos = getPos(e);
            scratch(pos.x, pos.y);
            e.preventDefault();
        }, { passive: false });
        scratchCanvas.addEventListener('touchend', () => { isScratching = false; });
        scratchCanvas.addEventListener('touchmove', (e) => {
            if (!isScratching) return;
            const pos = getPos(e);
            scratch(pos.x, pos.y);
            e.preventDefault();
        }, { passive: false });
    }

    // --- 5. Falling Petals (Mindblowing effect) ---
    const particlesContainer = document.getElementById('particles');
    const createParticle = () => {
        const particle = document.createElement('div');
        particle.classList.add('petal');

        // Randomize size and starting horizontal position
        const size = Math.random() * 15 + 10;
        particle.style.width = size + 'px';
        particle.style.height = size + 'px';
        particle.style.left = Math.random() * 100 + 'vw';

        // Advanced physics animation
        const duration = Math.random() * 6 + 6; // Falls over 6-12 seconds
        const sway = Math.random() * 150 - 75; // Sways left/right
        const rotations = Math.random() * 720; // Spins

        particle.animate([
            { transform: 'translateY(-10vh) translateX(0) rotate(0deg)', opacity: 0 },
            { opacity: 0.8, offset: 0.1 },
            { opacity: 0.8, offset: 0.9 },
            { transform: `translateY(110vh) translateX(${sway}px) rotate(${rotations}deg)`, opacity: 0 }
        ], {
            duration: duration * 1000,
            easing: 'ease-in-out',
            fill: 'forwards'
        });

        particlesContainer.appendChild(particle);

        // Cleanup DOM
        setTimeout(() => {
            particle.remove();
        }, duration * 1000);
    };

    setInterval(createParticle, 400);

    // --- 6. Family Invites Slider ---
    let slideIndex = 1;
    let slideInterval;

    const showSlides = (n) => {
        let i;
        let slides = document.getElementsByClassName("slide");
        let dots = document.getElementsByClassName("dot");
        if (slides.length === 0) return;

        if (n > slides.length) { slideIndex = 1 }
        if (n < 1) { slideIndex = slides.length }

        for (i = 0; i < slides.length; i++) {
            slides[i].style.display = "none";
        }
        for (i = 0; i < dots.length; i++) {
            dots[i].className = dots[i].className.replace(" active", "");
        }

        slides[slideIndex - 1].style.display = "block";
        dots[slideIndex - 1].className += " active";
    };

    const startSlideTimer = () => {
        clearInterval(slideInterval);
        slideInterval = setInterval(() => {
            slideIndex++;
            showSlides(slideIndex);
        }, 3000); // 3 seconds
    };

    window.changeSlide = (n) => {
        showSlides(slideIndex += n);
        startSlideTimer(); // Reset timer on manual interaction
    };

    window.currentSlide = (n) => {
        showSlides(slideIndex = n);
        startSlideTimer(); // Reset timer on manual interaction
    };

    // Initialize slider
    showSlides(slideIndex);
    startSlideTimer();

    // --- 7. 3D Tilt Physics for Tall Cards ---
    const tallCards = document.querySelectorAll('.tall-card');
    tallCards.forEach(card => {
        card.addEventListener('mousemove', (e) => {
            const rect = card.getBoundingClientRect();
            const x = e.clientX - rect.left; // mouse x within card
            const y = e.clientY - rect.top;  // mouse y within card
            const centerX = rect.width / 2;
            const centerY = rect.height / 2;
            const rotateY = ((x - centerX) / centerX) * 15; // max 15deg
            const rotateX = ((centerY - y) / centerY) * 10; // max 10deg
            card.style.transform = `perspective(1000px) rotateX(${rotateX}deg) rotateY(${rotateY}deg) scale(1.02)`;
            card.style.boxShadow = `${-rotateY * 2}px ${rotateX * 2}px 60px rgba(0,0,0,0.5), 0 0 30px rgba(212,175,55,0.2)`;
        });

        card.addEventListener('mouseleave', () => {
            card.style.transform = 'perspective(1000px) rotateX(0) rotateY(0) scale(1)';
            card.style.boxShadow = '0 20px 60px rgba(0,0,0,0.6), 0 0 30px rgba(212,175,55,0.15)';
        });

        // Touch support for mobile
        card.addEventListener('touchmove', (e) => {
            const touch = e.touches[0];
            const rect = card.getBoundingClientRect();
            const x = touch.clientX - rect.left;
            const y = touch.clientY - rect.top;
            const centerX = rect.width / 2;
            const centerY = rect.height / 2;
            const rotateY = ((x - centerX) / centerX) * 12;
            const rotateX = ((centerY - y) / centerY) * 8;
            card.style.transform = `perspective(1000px) rotateX(${rotateX}deg) rotateY(${rotateY}deg) scale(1.02)`;
        }, { passive: true });

        card.addEventListener('touchend', () => {
            card.style.transform = 'perspective(1000px) rotateX(0) rotateY(0) scale(1)';
            card.style.boxShadow = '0 20px 60px rgba(0,0,0,0.6), 0 0 30px rgba(212,175,55,0.15)';
        });
    });

    // --- 8. Card Particle Effects (Flowers, Fireflies, Sparkles) ---
    document.querySelectorAll('.card-particles').forEach(container => {
        const effect = container.getAttribute('data-effect');

        if (effect === 'flowers') {
            for (let i = 0; i < 15; i++) {
                const flower = document.createElement('div');
                flower.classList.add('card-flower');
                flower.style.left = Math.random() * 100 + '%';
                flower.style.animationDuration = (Math.random() * 6 + 6) + 's';
                flower.style.animationDelay = (Math.random() * 8) + 's';
                const size = Math.random() * 10 + 8;
                flower.style.width = size + 'px';
                flower.style.height = size + 'px';
                const colors = [
                    'radial-gradient(ellipse, #ff9fb0 30%, #e76f8a 100%)',
                    'radial-gradient(ellipse, #ffcc5c 30%, #f7a134 100%)',
                    'radial-gradient(ellipse, #ffd9e0 30%, #ff8da1 100%)',
                    'radial-gradient(ellipse, #fff5b0 30%, #f0d060 100%)'
                ];
                flower.style.background = colors[Math.floor(Math.random() * colors.length)];
                container.appendChild(flower);
            }
        }

        if (effect === 'fireflies') {
            for (let i = 0; i < 10; i++) {
                const firefly = document.createElement('div');
                firefly.classList.add('card-firefly');
                firefly.style.left = Math.random() * 90 + 5 + '%';
                firefly.style.top = Math.random() * 70 + 10 + '%';
                firefly.style.animationDuration = (Math.random() * 5 + 4) + 's';
                firefly.style.animationDelay = (Math.random() * 6) + 's';
                const size = Math.random() * 4 + 3;
                firefly.style.width = size + 'px';
                firefly.style.height = size + 'px';
                container.appendChild(firefly);
            }
        }

        if (effect === 'sparkles') {
            for (let i = 0; i < 25; i++) {
                const sparkle = document.createElement('div');
                sparkle.classList.add('card-sparkle');
                sparkle.style.left = Math.random() * 100 + '%';
                sparkle.style.top = Math.random() * 80 + '%';
                sparkle.style.animationDuration = (Math.random() * 3 + 2) + 's';
                sparkle.style.animationDelay = (Math.random() * 5) + 's';
                container.appendChild(sparkle);
            }
        }
    });
});
